services:
  finances:
    build:
      context: https://github.com/jgoedde/Finances.git#${FINANCES_VERSION}
      dockerfile: Dockerfile
    image: finances-client:${FINANCES_VERSION}
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true
