services:
  music-stats:
    image: halverneus/static-file-server:1.8.12
    container_name: music-stats
    restart: unless-stopped
    environment:
      FOLDER: /web
      CORS: "true"
    volumes:
      - /home/julian/Music/stats:/web:ro
    ports:
      - "127.0.0.1:8091:8080"
